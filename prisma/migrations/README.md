# Prisma migrations (SQLite — local development archive)

SQLite migration history for local development. Do not apply to Neon PostgreSQL.

Neon PostgreSQL migrations live in `prisma/migrations_postgresql/`.
