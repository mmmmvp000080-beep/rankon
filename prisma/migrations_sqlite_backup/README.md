# SQLite migration backup

These migrations were used for local SQLite development before the PostgreSQL (Neon) transition.

Do not apply these files to Neon PostgreSQL.

They are preserved for reference only.
